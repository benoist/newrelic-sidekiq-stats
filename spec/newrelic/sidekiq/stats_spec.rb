RSpec.describe Newrelic::Sidekiq::Stats do
  it "has a version number" do
    expect(Newrelic::Sidekiq::Stats::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
